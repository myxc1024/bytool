/**
 * @description 字符串替换
 * @version 1.0.0
 * @param { String } str 要替换的字符串
 * @param { String } AFindText 要提替换的字符
 * @param { String } ARepText 替换后的字符
 * @returns { String } 替换后完整的字符串
 */
export declare const replaceAll: (str: string, AFindText: string, ARepText: string) => string;
